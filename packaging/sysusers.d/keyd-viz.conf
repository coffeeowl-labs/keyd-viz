# Dedicated unprivileged system user for keydviz-helperd. No home, no shell, no login.
# Group membership (keyd, input) is granted by the service unit via SupplementaryGroups,
# not here, so the user owns no standing access on its own.
u keyd-viz - "keyd-viz broker daemon" - -
